[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "svitch"
version = "0.1.2"
description = "DPDP-compliant AI data security — PII detection, redaction, and agent audit trails for Indian enterprises."
readme = "README.md"
license = { text = "Apache-2.0" }
requires-python = ">=3.10"
keywords = ["pii", "dpdp", "llm", "security", "india", "compliance", "aadhaar", "pan", "agent", "audit"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: Apache Software License",
    "Programming Language :: Python :: 3",
    "Topic :: Security",
    "Topic :: Software Development :: Libraries",
]
dependencies = []

[project.optional-dependencies]
openai = ["openai>=1.0.0"]
anthropic = ["anthropic>=0.25.0"]
all = ["openai>=1.0.0", "anthropic>=0.25.0"]

[project.urls]
Homepage = "https://svitch.ai"
Documentation = "https://svitch.ai/dpdp"
Repository = "https://github.com/koushiknarendra/svitch"
Issues = "https://github.com/koushiknarendra/svitch/issues"

[tool.hatch.build.targets.wheel]
packages = ["svitch", "svitch_tracer"]
